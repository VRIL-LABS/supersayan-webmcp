{
  "name": "glow-chrome-extension",
  "version": "1.0.0",
  "description": "Glow — WebMCP Security Shield Chrome Extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.5",
    "@webgpu/types": "^0.1.71",
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0"
  }
}
